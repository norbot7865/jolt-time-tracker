# Configuration adapter

Reserved for desktop XDG/legacy configuration discovery, migration, atomic
private persistence, and environment overrides. It must not expose secrets in
messages or logging and does not own Android secure storage.
