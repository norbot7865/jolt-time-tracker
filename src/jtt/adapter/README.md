# Adapters

Adapters translate external APIs, persistence, and transport into canonical port
contracts. Provider-specific headers, URLs, IDs, DTOs, and timestamps stay below
this boundary. Each child documents its narrower responsibility.
