# Application

Provider-neutral workflows coordinate pure domain rules through injected port
capabilities. This layer owns lifecycle semantics such as partial continue
results, not provider DTOs or frontend widget state.
