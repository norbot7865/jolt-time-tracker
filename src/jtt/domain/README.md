# Domain

Pure time-tracker values, validation, interval arithmetic, local-day totals,
and toolkit-free view-model derivation belong here. It must not import providers,
HTTP, persistence, UI toolkits, JNI, or process-global environment state.
